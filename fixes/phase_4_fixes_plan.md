Round kind: bug fixes

# Round 4 — the re-audit findings (found and fixed in one pass)

21 findings from the four-surface re-audit, every one fixed and verified the
same session; the item details live on tasks T-065..T-085. Highlights: the
palette mis-selection P1, the failed-round toast storm, archived tasks swept
into rounds, and the honesty cluster (stale banners, false just-done, wrong
clocks). Full 16-suite regression sweep + 19 backend tests green after.
